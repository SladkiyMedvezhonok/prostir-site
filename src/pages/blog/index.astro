---
import Layout from "../../layouts/Layout.astro";

Astro.response.headers.set('Content-Type', 'text/html; charset=utf-8');

const allPosts = [
  {
    bold: "Емоційне вигорання:",
    rest: " як розпізнати та що робити",
    url: "/blog/vygorannya"
  },
  {
    bold: "Гіпнотерапія:",
    rest: " сучасний метод психологічної підтримки",
    url: "/blog/hipnosis"
  },

  {
  bold: "Тривожність:",
  rest: " прості техніки самодопомоги",
  url: "/blog/tryvozhnost"
}
  
];
---

<Layout>
  <section class="blog-section">
    <h1>Блог психолога</h1>

    <div class="card-grid">
      {allPosts.map((post) => (
        <a href={post.url} class="blog-card">
     <h2>
  {post.bold ? <><strong>{post.bold}</strong>{post.rest}</> : post.title}
</h2>
          <div class="overlay">Читати статтю</div>
        </a>
      ))}
    </div>
  </section>
</Layout>

<style>
  /* 📦 Контейнер секції */
  .blog-section {
    max-width: 1100px; /* 🔁 Рекомендовано: 960px–1200px для зручності читання */
    margin: 0 auto; /* Центрує контент */
    padding: 60px 20px; /* 🔁 Внутрішні відступи: top-bottom 60px, left-right 20px */
    text-align: center; /* Вирівнювання заголовка по центру */
  }

  .blog-section h1 {
    font-size: 2rem; /* 🔁 Рекомендовано: 1.5rem–3rem (24–48px) */
    margin-bottom: 40px; /* 🔁 Відступ після заголовка: 24–60px */
  }

  /* 🧱 Сітка плиток */
  .card-grid {
    display: flex; /* Вивід блоків у ряд */
    flex-wrap: wrap; /* Переносить картки на новий рядок при потребі */
    justify-content: center; /* Центрує плитки */
    gap: 1.5rem; /* 🔁 Відстань між плитками: 1rem–2rem */
  }

  /* 🔳 Стиль кожної картки */
  .blog-card {
    width: 280px; /* 🔁 Ширина плитки: 260px–320px */
    height: 50px; /* 🔁 Висота плитки: 140px–220px — підходить для 2 рядків тексту + відступи */
    background-color: white; /* 🔁 Можна змінити на #fdfdfd або #fafafa для мʼякості */
    border-radius: 1.5rem; /* 🔁 Радіус заокруглення: 0.5rem–2rem */
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08); /* 🔁 Мʼяка тінь: регулюй прозорість та offset */
    text-decoration: none;
    color: #111; /* 🔁 Основний колір тексту: #111, #222, #333 */
    padding: 1.2rem; /* 🔁 Внутрішні відступи: 1rem–1.5rem */
    position: relative; /* Потрібно для абсолютної overlay */
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Плавний ефект при наведенні */
    display: flex;
    align-items: flex-start;
    justify-content: flex-start;
  }

  .blog-card:hover {
    transform: translateY(-6px); /* 🔁 Підстрибування: -2px до -8px */
    box-shadow: 0 16px 32px rgba(0, 0, 0, 0.15); /* 🔁 Посилена тінь на ховері */
  }

  /* 📝 Заголовок картки */
  .blog-card h2 {
    font-size: 1rem; /* 🔁 Розмір тексту: 0.9rem–1.25rem */
    font-weight: 400; /* 🔁 100–400 — зробить текст нормальним (не жирним) */
    line-height: 1.4; /* 🔁 Висота рядка для читабельності */
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2; /* 🔁 Обмеження до 2 рядків */
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* 🌘 Затемнення при наведенні */
  .overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.4); /* 🔁 Прозорість: 0.2 (світло) — 0.6 (темно) */
    color: white;
    font-size: 0.9rem; /* 🔁 Розмір тексту кнопки: 0.8rem–1rem */
    font-weight: 500; /* 🔁 400–600 — читабельність */
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    border-radius: 1.5rem;
    transition: opacity 0.3s ease; /* 🔁 Плавна поява */
  }

  .blog-card:hover .overlay {
    opacity: 1;
  }
  

  /* 📱 Мобільна адаптація */
  @media (max-width: 768px) {
    .blog-card {
      width: 100%; /* 🔁 Займає всю ширину контейнера */
      height: auto; /* 🔁 Автовисота, якщо контент перевищить */
    }

    .card-grid {
      gap: 1rem; /* 🔁 Зменшене міжкарткове поле на мобільних */
      padding: 0 10px;
    }

    .blog-card h2 {
      font-size: 1rem; /* 🔁 Трохи менший текст на телефонах */
      -webkit-line-clamp: 3; /* 🔁 Можна дозволити 3 рядки */
    }
  }
</style>

